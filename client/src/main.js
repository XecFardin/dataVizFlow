import { createApp } from 'vue'; // Import the createApp function from Vue
import App from './App.vue'; // Import the root component

// Create the Vue app instance and mount the root component to the element with the id 'app'
createApp(App).mount('#app');
